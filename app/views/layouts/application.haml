<!DOCTYPE html>
%html
  %head
    %title Iriomote
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header
      #user_nav
        - if current_user
          Logged in as #{current_user.email}; #{link_to 'Sign out', log_out_path}
        - else
          = link_to 'Sign up', sign_up_path
          or
          = link_to 'Log in', log_in_path
    - flash.each do |name, msg|
      = content_tag :div, msg, :id => "flash_#{name}"
    #page
      = yield
    %footer
      Copyright &copy; 2012, 2013 xc-group et al.
